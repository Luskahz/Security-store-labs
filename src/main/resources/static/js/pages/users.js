import {api} from '../api.js';import {adminPage,escapeHtml,message,has} from '../auth.js';
try{const me=await adminPage('Usuários');if(!has(me,'IDENTITY_USER_READ'))throw new Error('Sem permissão para consultar usuários');async function load(){const users=await api('/admin/users');document.querySelector('#table').innerHTML=users.map(u=>`<tr><td><a href="/admin/user.html?id=${u.id}">${escapeHtml(u.name)}</a></td><td>${escapeHtml(u.email)}</td><td>${escapeHtml(u.status)}</td><td>${u.roles.map(escapeHtml).join(', ')}</td><td>${has(me,u.status==='ACTIVE'?'IDENTITY_USER_DISABLE':'IDENTITY_USER_ENABLE')?`<button data-id="${u.id}" data-action="${u.status==='ACTIVE'?'disable':'enable'}" class="secondary">${u.status==='ACTIVE'?'Desabilitar':'Habilitar'}</button>`:''}</td></tr>`).join('');document.querySelectorAll('[data-action]').forEach(button=>button.onclick=async()=>{try{await api(`/admin/users/${button.dataset.id}/${button.dataset.action}`,{method:'PATCH'});await load()}catch(e){message(e.message,true)}})}document.querySelector('#content').innerHTML=`${has(me,'IDENTITY_USER_CREATE')?'<section class="card"><h2>Criar usuário</h2><form id="create"><div class="grid"><label>Nome<input name="name" required></label><label>E-mail<input name="email" type="email" required></label><label>Senha<input name="password" type="password" minlength="8" required></label></div><button>Criar</button></form></section>':''}<section class="card"><table><thead><tr><th>Nome</th><th>E-mail</th><th>Status</th><th>Roles</th><th>Ações</th></tr></thead><tbody id="table"></tbody></table></section>`;document.querySelector('#create')?.addEventListener('submit',async event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.target));try{await api('/admin/users',{method:'POST',body:JSON.stringify(data)});event.target.reset();message('Usuário criado');await load()}catch(e){message(e.message,true)}});await load()}catch(e){message(e.message,true)}
